# Pages supplies its native SPA fallback when this project has no top-level
# 404.html. Do not add an explicit /* rewrite: clean-URL handling turns that
# rule into an index.html redirect loop in the local and production runtime.
